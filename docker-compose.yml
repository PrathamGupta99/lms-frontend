version: '3.9'
services:
  frontend:
    build:
      context: .
    env_file:
      - ./.env
    environment:
      - NEXT_PUBLIC_API_URL=${NEXT_PUBLIC_API_URL:-http://localhost:4000}
    ports:
      - '3000:3000'
    restart: unless-stopped
